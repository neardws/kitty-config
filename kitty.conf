# Kitty 配置文件

# 引入主题
include ./theme.conf

# 启用远程控制（用于实时预览主题）
allow_remote_control yes

# 窗口设置
window_padding_width 4
hide_window_decorations no

# 启用鼠标
mouse_hide_wait 3.0

# 终端类型
term xterm-kitty

# 字体设置
font_family      family=BerkeleyMapleMono postscript_name=BerkeleyMapleMono-Regular
bold_font        family=BerkeleyMapleMono postscript_name=BerkeleyMapleMono-Bold
italic_font      family=BerkeleyMapleMono postscript_name=BerkeleyMapleMono-Italic
bold_italic_font auto
font_size 14.0
# END_KITTY_FONTS


# ===== 布局设置 =====
enabled_layouts splits,stack


# ===== 快捷键配置 (macOS) =====

# --- 分屏 ---
# cmd+d: 水平分屏（上下，新窗口在下方）
# cmd+r: 垂直分屏（左右，新窗口在右侧）
map cmd+d launch --location=hsplit 
map cmd+r launch --location=vsplit

# --- 移动窗口位置 ---
map cmd+shift+up move_window up
map cmd+shift+down move_window down
map cmd+shift+left move_window left
map cmd+shift+right move_window right

# ===== Tab Bar 设置 =====
tab_bar_edge bottom
tab_bar_style custom
tab_bar_min_tabs 1
tab_bar_margin_height 5.0 0.0
tab_bar_background #1e1e2e

# Tab 颜色 (Soft Pastel 风格)
active_tab_foreground   #1e1e2e
active_tab_background   #cdd6f4
active_tab_font_style   bold
inactive_tab_foreground #cdd6f4
inactive_tab_background #313244
inactive_tab_font_style normal

# --- Tab 管理 ---
map cmd+t new_tab
map cmd+w close_window
map cmd+shift+[ previous_tab
map cmd+shift+] next_tab
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9

# --- 焦点切换 ---
map cmd+[ previous_window
map cmd+] next_window
map cmd+j neighboring_window left
map cmd+m neighboring_window down
map cmd+i neighboring_window up
map cmd+k neighboring_window right

# --- 窗口管理 ---
map cmd+shift+w close_tab
map cmd+shift+r start_resizing_window

# --- 其他常用 ---
map cmd+c copy_to_clipboard
map cmd+v paste_from_clipboard
map cmd+equal change_font_size all +1.0
map cmd+minus change_font_size all -1.0
map cmd+0 change_font_size all 0
map cmd+enter toggle_fullscreen
map cmd+n new_os_window
